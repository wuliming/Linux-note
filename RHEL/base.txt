1) how to see thread infomation of process
# ps -T -p 3171
  PID  SPID TTY          TIME CMD
 3171  3171 pts/0    00:00:50 iperf3
#  pstree -p 2
kthreadd(2)-+-ata_sff(295)
            |-bioset(82)
            |-bioset(92)
2) how to free cached memory
before free had better to do sync
# sync
To free pagecache:
# echo 1 > /proc/sys/vm/drop_caches
To free dentries and inodes:
# echo 2 > /proc/sys/vm/drop_caches
To free pagecache, dentries and inodes:
# echo 3 > /proc/sys/vm/drop_cachesTo free pagecache:

3) how to calculation
* intger calculation
$(( 3 * 5 ))
* float calculation
# echo "2 * 2.6" | bc
* radication calculation
# echo "sqrt(10.000)" | bc
# awk "BEGIN { x=sqrt(10); print x }"
* sum calculation
cat num.txt | awk "{ sum += $1 } END { print sum }"
